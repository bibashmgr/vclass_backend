const httpStatus = require('http-status');

// utils
const logger = require('../utils/logger.js');

const createMessage = async (req, res) => {};

const getMessages = async (req, res) => {};

modules.exports = { createMessage, getMessages };
